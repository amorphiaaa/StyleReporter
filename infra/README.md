# Infrastructure scaffold

The current local infrastructure entry point is the root docker-compose.yml.

This directory is reserved for future infrastructure concerns such as:

- database initialization and local seed helpers;
- deployment manifests;
- environment-specific configuration;
- operational runbooks.

No production deployment or CI/CD configuration is included in the scaffold.
