# ADR 0001: Keep the first repository change to scaffolding

## Status

Accepted

## Decision

The first repository change provides structure, configuration, documentation,
interfaces, placeholders, and synthetic fixtures only. It does not implement
the product workflow.

## Consequences

The repository is safe to hand off before provider credentials, client data,
methodology prompts, and persistence rules are finalized.
